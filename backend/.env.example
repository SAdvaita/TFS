# Backend environment configuration
DATABASE_URL="file:./dev.db"
PORT=5000
JWT_SECRET="replace_with_a_long_random_secret"

# Optional: if you later move to PostgreSQL/MySQL
# DATABASE_URL="postgresql://user:password@localhost:5432/tfs"
